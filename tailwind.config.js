module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      colors: {
        primary: "#DDD0C8", // Beige
        accent: "#323232", // Dark Gray
        background: "#F5F5F5", // Light Gray (if needed)
        buttonHighlight: "#A8E6CF", // Mint Green (if still needed)
        textPrimary: "#4A4A4A", // Dark Gray for text
      },
    },
  },
  plugins: [],
};
